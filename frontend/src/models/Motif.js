// models/Motif.js
class Motif {
    constructor(idMotif, nbMinApparitionMotif, nbMaxApparitionMotif) {
        this.idMotif = idMotif;
        this.nbMinApparitionMotif = nbMinApparitionMotif;
        this.nbMaxApparitionMotif = nbMaxApparitionMotif;
    }
}

export default Motif;
